language: node_js
node_js:
  - 'stable'
cache: npm
script:
  - echo "Building"
branches:
  only:
    - master
install:
  - yarn build
  - npm install -g firebase-tools
after_success:
  - firebase deploy --project awk-dating --token $FIREBASE_TOKEN
